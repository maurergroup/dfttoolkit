name: ruff

on:
  push:
    branches:
      - main

  pull_request:
    branches:
      - main

jobs:
  ruff-lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          src: "./dfttoolkit"
          args: "check --diff --output-format github --config ./pyproject.toml --no-preview"

  ruff-format:
    runs-on: ubuntu-latest
    needs: ruff-lint
    steps:
      - uses: actions/checkout@v4
      - uses: astral-sh/ruff-action@v3
        with:
          src: "./dfttoolkit"
          args: "format --check --target-version py311 --config ./pyproject.toml"
